## Nombre de sièges

```plotly
function getConseillersGraphData() {
    // 1. Données numériques pour un espacement proportionnel
    const population = [0, 100, 500, 1500, 2500, 3500, 5000, 10000, 20000, 30000, 40000, 50000, 60000, 80000, 100000, 150000, 200000, 250000, 300000, 350000];
    const conseillers = [7, 7, 11, 15, 19, 23, 27, 29, 33, 35, 39, 43, 45, 49, 53, 55, 59, 61, 65, 69];

    // 2. Définition de la trace (le graphique)
    const trace = {
        x: population,
        y: conseillers,
        type: 'scatter',
        mode: 'lines+markers',
        name: 'Conseillers',
        line: {
            shape: 'vh', // 'vh' force le tracé en escalier (Vertical-Horizontal)
            color: '#1f77b4',
            width: 3
        },
        marker: {
            size: 6,
            color: '#1f77b4'
        },
        hovertemplate: '<b>Population :</b> %{x} hab.<br><b>Conseillers :</b> %{y}<extra></extra>'
    };

    // 3. Définition du layout (la mise en page)
    const layout = {
        xaxis: {
            title: "Nombre d'habitants",
            type: 'log', // Ajout de cette ligne si besoin
            gridcolor: '#E1E1E1',
            zeroline: false,
        },
        yaxis: {
            title: 'Nombre de conseillers',
            gridcolor: '#E1E1E1',
            zeroline: false
        },
        margin: { l: 50, r: 30, t: 50, b: 50 },
        plot_bgcolor: '#FAFAFA'
    };

    const config = {
            responsive: true,
            displayModeBar: false // Optionnel : masque la barre d'outils Plotly pour un rendu plus propre sur vos slides
        };

    // 4. On retourne l'objet JSON attendu par Plotly / SliDesk
    return {
        data: [trace],
        layout,
        config
    };
}
```

**Lyon** : 73 / **Marseille** : 111 / **Paris** : 163
